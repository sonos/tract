// vim: set syntax=asm :

{{L}}load_tile:
    mov          r8, [rdi + 8]
    {% for reg in range(from, to + 1) %}
        vmovups         zmm{{reg}}, zmmword ptr [r8 + {{ (reg - from) * 64 }}]
    {% endfor %}

    jmp    {{L}}non_linear_loop

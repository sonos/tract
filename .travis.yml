sudo: true
language: rust
cache:
  - cargo
  - directories:
    - cached

rust:
- stable
- beta
- nightly
env:
- RUST_BACKTRACE=1
script:
-  cargo build
-  cargo test
-  cd conform ; cargo test
-  cargo bench
-  cargo doc
matrix:
  allow_failures:
  - rust: nightly

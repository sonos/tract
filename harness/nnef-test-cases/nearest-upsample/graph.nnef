version 1.0;

extension tract_registry tract_core;

graph nearest_upsample_hw(input) -> (output)
{
    input  = external<scalar>(shape = [1, 3, 5, 7]);
    output = nearest_upsample(input, factor = [2, 3]);
}

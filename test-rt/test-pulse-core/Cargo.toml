[package]
name = "test-pulse-core"
version = "0.1.0"
edition = "2024"

[dependencies]
tract-core.workspace = true

[dev-dependencies]
suite-pulse = { path = "../suite-pulse" }

[build-dependencies]
suite-pulse = { path = "../suite-pulse" }
